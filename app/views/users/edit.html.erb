<% if user_signed_in? %>
  <% if current_user == @user || current_user.is_admin? %>
   <h1>Paramétrage compte Utilisateur</h1>

    <%= render 'form' %>

    <%= link_to 'Show', @user %> |
    <%= link_to 'Back', users_path %>
  <% end %>
<% end %>
