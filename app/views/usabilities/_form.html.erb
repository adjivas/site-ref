<%= form_for(@usability) do |f| %>
  <% if @usability.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@usability.errors.count, "error") %> prohibited this usability from being saved:</h2>

      <ul>
      <% @usability.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
