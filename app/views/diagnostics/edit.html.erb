<article>
  <% if user_signed_in? && current_user.is_admin? %>
    <h1>Modifier un diagnostique</h1>

    <%= render 'form' %>

    <%= link_to 'Voir', @diagnostic %> |
    <%= link_to 'Retour', diagnostics_path %>
  <% end %>
</article>
