<article>
  <% if user_signed_in? && current_user.is_admin? %>
    <p id="notice"><%= notice %></p>

    <p>
      <strong>Nom :</strong>
      <%= @language.name %>
    </p>

    <%= link_to 'Éditer', edit_language_path(@language) %> |
    <%= link_to 'Retour', languages_path %>
  <% end %>
</article>
