<%= form_for(@type_map) do |f| %>
  <% if @type_map.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_map.errors.count, "error") %> prohibited this type_map from being saved:</h2>

      <ul>
      <% @type_map.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
