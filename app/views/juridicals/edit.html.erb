<article>
  <% if user_signed_in? && current_user.is_admin? %>
    <h1>Modifier le type d'établissement</h1>

    <%= render 'form' %>

    <%= link_to 'Voir', @juridical %> |
    <%= link_to 'Retour', juridicals_path %>
  <% end %>
</article>
