<article>
  <% if user_signed_in? && current_user.is_admin? %>
    <p id="notice"><%= notice %></p>

    <p>
      <strong>Nom :</strong>
      <%= @juridical.name %>
    </p>

    <%= link_to 'Éditer', edit_juridical_path(@juridical) %> |
    <%= link_to 'Retour', juridicals_path %>
  <% end %>
</article>
